/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['github.com', 'avatars.githubusercontent.com', 'encrypted-tbn0.gstatic.com'],
  },
}

module.exports = nextConfig